# API Configuration
# For local development on native devices, use your machine's local IP
# For web/emulator, localhost will be used automatically

# API Host - your machine's local IP or domain
EXPO_PUBLIC_API_HOST=192.168.1.221

# API Port
EXPO_PUBLIC_API_PORT=5132

# API endpoints (constructed from host and port)
# Used by the app to determine correct URL based on platform
